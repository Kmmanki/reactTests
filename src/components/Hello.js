import React from 'react'

const Hello = ({name, fn}) => {
    // const  {fn,name } = props;
    return (
    // <h1 onClick ={() => fn(name)}>{name}</h1>
    <h1>name</h1>
    )
}
export default Hello

